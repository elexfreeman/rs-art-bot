# =============================
# rs-bot-art — .env.example
# Скопируйте этот файл как `.env` и подставьте свои значения.
# Пример: cp .env.example .env
# =============================

# --- Telegram Bot ---
# Токен бота от @BotFather. Обязательно.
TELOXIDE_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# --- Канал для публикаций ---
# Числовой ID канала в формате -100xxxxxxxxxxxx. Не username!
# Как получить: добавьте бота админом в канал, затем перешлите любое
# сообщение из канала боту @userinfobot или @getidsbot — он вернёт chat_id.
# Можно оставить пустым и задать командой /set_channel в ЛС боту.
CHANNEL_ID=-1001234567890123

# --- OpenAI (генерация текста) ---
# Ключ OpenAI. Если пусто — бот использует локальный генератор как fallback.
OPENAI_API_KEY=sk-your-openai-key

# Модель для обычной (текстовой) генерации. По умолчанию: gpt-4o-mini
# Можно указать, например: gpt-4o, gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Базовый URL API. По умолчанию: https://api.openai.com
# Поменяйте, если используете прокси/совместимый endpoint.
OPENAI_BASE=https://api.openai.com

# --- Vision (анализ изображения) ---
# Включить отправку превью изображения в OpenAI (Vision) для более точного текста.
# true/false (или 1/0). Если false — будет только текстовая генерация.
OPENAI_USE_VISION=true

# Модель для Vision. Примеры: gpt-4o, gpt-4o-mini
# Если не указано — используется OPENAI_MODEL.
OPENAI_VISION_MODEL=gpt-4o

# Примечания:
# - Бот публикует фото в канал, переиспользуя file_id (из TG), без повторной загрузки.
# - Vision режим кодирует изображение в base64 и отправляет в Chat Completions как data URL.
# - Текст ограничивается до ~1000 символов, чтобы уложиться в лимит подписи Telegram (1024).

# --- Планировщик публикаций ---
# Включите один из вариантов расписания:
# 1) Интервал в секундах. Если > 0 — используется и ПЕРЕОПРЕДЕЛЯЕТ cron.
POST_INTERVAL_SECS=0
# 2) Крон в формате "M H * * *". Примеры:
#    Каждый час в нулевую минуту: 0 * * * *
#    Каждый день в 10:30:          30 10 * * *
# Если POST_INTERVAL_SECS>0, то POST_CRON игнорируется.
POST_CRON=* * * * *

# --- Файлы и база данных ---
# Папка, откуда брать изображения для авто-постинга.
FILES_DIR=files
# Путь к SQLite базе данных.
DB_PATH=bot.db
OPENAI_SYSTEM_PROMPT="..."        # системный промпт для Vision-генерации подписи
